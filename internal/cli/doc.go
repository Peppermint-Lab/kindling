// Package cli provides shared HTTP client and filesystem config for the Kindling remote CLI.
//
// Configuration is stored at ~/.kindling/cli-config.json (override with KINDLING_CLI_CONFIG).
// Roadmap-aligned follow-ups (not implemented here): kindling.toml defaults, init scaffolding,
// stdin-based secret upserts, and broader command parity with the dashboard.

package cli
